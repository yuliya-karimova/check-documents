# "Свет в темном лесу требований" 🌲✨

Наше решение — это ваш надёжный "фонарик", который проложит путь через густые дебри регламентов и сложных стандартов! 🕵️‍♀️🔦 Благодаря мощным ИИ-моделям, мы делаем анализ документации быстрым, точным и управляемым.

🔹 **Инновационный подход:** Наш инструмент использует передовые технологии, такие как FLAN-T5, LLaMA и RoBERTa, чтобы обеспечить автоматизацию и мгновенный анализ. Это не просто обработка данных — это новый уровень комплаенс-контроля, который адаптируется к вашим требованиям и условиям. 🚀

🔹 **Молниеносный анализ:** Сложные вопросы разбиваются на простые, а нерелевантная информация отсекается, оставляя только важное. Это позволяет сосредоточиться на сути и экономит ваше время. ⚙️💡

🔹 **Защита от "фантазирования":** Наш инструмент гарантирует, что результат основан исключительно на фактах, а не на догадках. Функции контроля соответствия и проверки обоснованности ответа помогают избежать ложных интерпретаций и поддерживать фокус на реальных данных. 🛡️✨

С КОСАТКА ANALYTICS 🌊 комплаенс-контроль становится прозрачным, удобным и предельно точным. Мы помогаем вам уверенно ориентироваться в "лесу" регламентов и видеть только то, что действительно важно! 📜🔍

Работать с программой очень просто:
1. Скачать LM Studio с сайта https://lmstudio.ai/, чтобы можно было локально загрузить большие языковые модели 
2. В программе скачать необходимые модели, запустить локальный сервер и скопировать id запущенной модели
3. Открыть файл check.ipynb и подставить в DEFAULT_MODEL id нужной модели
4. Запустить блокнот

Либо воспользоваться развернутым сервисом по адресу http://141.105.64.87:8000/home/


# Описание проекта

Данный проект предназначен для автоматизированной проверки соответствия описания человеко-машинного интерфейса (HMI) требованиям системных стандартов и спецификаций (SSTS). Программа выполняет следующие основные функции:

1. **Загрузка и обработка документов**: Извлечение информации из документов формата `.docx` в указанных папках.
2. **Сопоставление данных**: Объединение данных из HMI и SSTS на основе номера документа.
3. **Анализ с использованием моделей искусственного интеллекта**: Применение локально развернутых моделей LLM для анализа и проверки соответствия.
4. **Генерация отчетов**: Формирование CSV-файла с результатами проверки соответствия и выявленными несоответствиями.

---

## Структура проекта

- **Загрузка документов**: Функция `load_documents_from_folder` загружает документы из указанных папок и извлекает необходимую информацию.
- **Обработка данных**: Объединение данных из HMI и SSTS в единый DataFrame для последующего анализа.
- **Анализ и проверка соответствия**: Использование моделей LLM для:
  - Создания краткого описания HMI.
  - Фильтрации нерелевантного контента.
  - Проверки соответствия требованиям SSTS.
  - Выявления и суммирования ключевых несоответствий.
  - Генерации интегрированного описания и оценки уровня соответствия.
- **Генерация итогового отчета**: Создание CSV-файла `submission.csv` с итоговыми результатами.

---

## Требования

- Python 3.x
- Установленные библиотеки:
  - `python-docx`
  - `pandas`
  - `re`
  - `transformers`
  - `openai`
  - `pydantic`
- Локально развернутая LLM-модель (например, LLaMA)
- Папки с документами:
  - `./HMI` — документы HMI с префиксом `UC-`
  - `./SSTS` — документы SSTS с префиксом `SSTS-`

---

## Установка и настройка

1. **Установите необходимые библиотеки**:

```bash
pip install python-docx pandas transformers openai pydantic
```

2. **Разверните локальную LLM-модель** и убедитесь, что она доступна по адресу `http://127.0.0.1:1234/v1/`.

3. **Подготовьте директории с документами**:

- Поместите файлы HMI в папку `./HMI` с именами, начинающимися на `UC-` и расширением `.docx`.
- Поместите файлы SSTS в папку `./SSTS` с именами, начинающимися на `SSTS-` и расширением `.docx`.

---

## Описание основных функций

### 1. Загрузка документов из папок

```python
def load_documents_from_folder(path, prefix):
    # Функция загружает документы из указанной папки и извлекает информацию
```

- **Назначение**: Загружает документы `.docx` из заданной папки, извлекает номер, название и полный текст документа.
- **Параметры**:
  - `path`: Путь к папке с документами.
  - `prefix`: Префикс имени файла для фильтрации (например, `UC-` или `SSTS-`).
- **Возвращает**: DataFrame с колонками `Number`, `Name` и `Text`.

### 2. Объединение данных HMI и SSTS

- Загружаются данные из папок `./HMI` и `./SSTS`.
- Объединение DataFrame `df_hmi` и `df_stss` по колонке `Number`.
- Обработка и переименование колонок для удобства дальнейшей работы.

### 3. Создание краткого описания HMI

```python
def create_short_hmi_description(text_hmi):
    # Функция создает краткое описание HMI с использованием модели LLM
```

- **Назначение**: Генерирует краткое и ясное описание HMI для облегчения дальнейшего анализа.
- **Параметры**:
  - `text_hmi`: Полный текст описания HMI.
- **Возвращает**: Краткое описание HMI.

### 4. Фильтрация нерелевантного контента из SSTS

```python
def keep_only_relevant_content(row):
    # Функция фильтрует Text_SSTS, оставляя только контент, релевантный Short HMI
```

- **Назначение**: Извлекает из SSTS только ту информацию, которая непосредственно относится к описанию HMI.
- **Параметры**:
  - `row`: Строка DataFrame с колонками `Short HMI` и `Text_SSTS`.
- **Возвращает**: Отфильтрованный текст требований SSTS.

### 5. Проверка соответствия HMI требованиям SSTS

```python
def answer_question_from_context(row):
    # Функция проверяет, соответствует ли Short HMI требованиям из Text_SSTS, используя цепочку рассуждений
```

- **Назначение**: Анализирует соответствие HMI требованиям и предоставляет обоснованный ответ.
- **Параметры**:
  - `row`: Строка DataFrame с необходимыми данными.
- **Возвращает**: Ответ с цепочкой рассуждений о соответствии.

### 6. Использование модели FLAN-T5 для проверки соответствия

```python
def check_compliance(short_hmi, text_ssts):
    # Функция определяет соответствие описания HMI техническому регламенту с помощью FLAN-T5
```

- **Назначение**: Использует модель FLAN-T5 для более детальной проверки соответствия.
- **Параметры**:
  - `short_hmi`: Краткое описание HMI.
  - `text_ssts`: Текст требований SSTS.
- **Возвращает**: Ответ о соответствии с указанием деталей.

### 7. Суммирование ключевых несоответствий

```python
def summarize_discrepancies(description):
    # Функция суммирует ключевые несоответствия в краткой форме
```

- **Назначение**: Создает краткое резюме основных проблем несоответствия.
- **Параметры**:
  - `description`: Полное описание несоответствий.
- **Возвращает**: Краткое описание ключевых несоответствий.

### 8. Создание интегрированного описания

```python
def create_description(row):
    # Функция объединяет информацию из HMI, SSTS и выявленных несоответствий в единое описание
```

- **Назначение**: Генерирует целостное описание, объединяющее основные функции, требования и несоответствия.
- **Параметры**:
  - `row`: Строка DataFrame с необходимыми данными.
- **Возвращает**: Интегрированное описание.

### 9. Проверка фактической обоснованности ответа

```python
def is_answer_grounded_on_context(state):
    # Функция определяет, основан ли ответ на фактах из контекста
```

- **Назначение**: Проверяет, содержит ли ответ только информацию из предоставленного контекста без добавления неподтвержденных данных.
- **Параметры**:
  - `state`: Словарь с контекстом и ответом.
- **Возвращает**: Результат проверки обоснованности ответа.

### 10. Оценка уровня соответствия

```python
def send_to_gpt_for_rating(row):
    # Функция присваивает уровень соответствия на основе анализа
```

- **Назначение**: Классифицирует общий уровень соответствия по заданным категориям (FC, LC, PC, NC, NA).
- **Параметры**:
  - `row`: Строка DataFrame с необходимыми данными.
- **Возвращает**: Двухбуквенное обозначение уровня соответствия.

### 11. Формирование итогового отчета

- Создается CSV-файл `submission.csv`, содержащий следующие колонки:
  - `Number`: Номер документа.
  - `Name`: Название документа.
  - `Differences`: Ключевые несоответствия.
  - `Description`: Интегрированное описание.
  - `Compliance Level`: Уровень соответствия.

---

## Использование

1. **Подготовьте документы** в папках `./HMI` и `./SSTS` с соответствующими префиксами в именах файлов.
2. **Запустите скрипт**, убедившись, что все необходимые библиотеки установлены и локальная LLM-модель доступна.
3. **Дождитесь завершения обработки**. В зависимости от объема данных и мощности вашего оборудования это может занять некоторое время.
4. **Ознакомьтесь с результатами** в файле `submission.csv`.

---

## Примечания

- **Безопасность данных**: Данные обрабатываются локально, и никакая информация не передается на внешние серверы.
- **Настройка моделей**: Убедитесь, что используемые модели совместимы с вашими данными. Возможно, потребуется адаптация или замена моделей на более подходящие.
- **Отладка**: В случае возникновения ошибок проверьте корректность путей к папкам и наличие всех необходимых файлов.
- **Замер скорости**: Файл блокнота с замером скорости лежит в корне проекте FULL_LAUNCH.ipynb

**Спасибо за использование данного инструмента!**
